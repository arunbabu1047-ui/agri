import { Alert, Box, Container, Grid, Link as MuiLink, Stack, Typography } from '@mui/material';
import MailOutlineRoundedIcon from '@mui/icons-material/MailOutlineRounded';
import { useTranslation } from '../../contexts/LanguageContext';

export default function ContactPage() { const { t } = useTranslation(); return <Container maxWidth="lg" className="page-shell"><Grid container spacing={6}><Grid size={{ xs: 12, md: 7 }}><Typography className="eyebrow">CONTACT / SAY HELLO</Typography><Typography variant="h1" className="inner-title">{t('contact.title')}</Typography><Typography color="text.secondary" sx={{ mt: 2, fontSize: '1.1rem', maxWidth: 580 }}>{t('contact.subtitle')}</Typography><Alert severity="info" variant="outlined" sx={{ mt: 4, maxWidth: 580 }}>{t('contact.note')}</Alert></Grid><Grid size={{ xs: 12, md: 5 }}><Box className="contact-card"><MailOutlineRoundedIcon color="primary" sx={{ fontSize: 34 }} /><Typography variant="h5" sx={{ mt: 2 }}>{t('contact.email')}</Typography><MuiLink href="mailto:hello@example.org" variant="h6">hello@example.org</MuiLink><Typography variant="h6" sx={{ mt: 4 }}>{t('contact.hours')}</Typography><Typography color="text.secondary">{t('contact.hoursValue')}</Typography><Stack direction="row" gap={1} sx={{ mt: 5 }}><span className="status-dot" /><Typography variant="body2" color="text.secondary">Configured by your team in environment settings</Typography></Stack></Box></Grid></Grid></Container>; }
